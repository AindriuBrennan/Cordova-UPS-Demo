import { AbstractPushRegistration } from "../AbstractPushRegistration";
import { OnMessageReceivedCallback, PushRegistrationOptions } from "../PushRegistration";
import { PushInitConfig, PushVariant } from "../PushInitConfig";
/**
 * AeroGear UPS registration SDK - Cordova based implementation
 *
 * Usage:

 * let registration = new PushRegistrationCordovaImpl(config);
 * registration.register("myAppleOrFirebaseToken");
 */
export declare class PushRegistrationCordovaImpl extends AbstractPushRegistration {
    static onMessageReceived(onMessageReceivedCallback: OnMessageReceivedCallback): void;
    private static onMessageReceivedCallback;
    private push?;
    constructor(config: PushInitConfig);
    /**
     * Register deviceToken for Android or IOS platforms
     *
     * @param options Push registration options
     */
    register(options?: PushRegistrationOptions): Promise<void>;
    /**
     * Unregister device for Android or IOS platforms
     */
    unregister(): Promise<void>;
    getPlatformConfig(pushConfig: PushInitConfig): PushVariant | undefined;
    protected validateConfig(): string | undefined;
    protected init(): void;
    private subscribeToFirebaseTopic;
    private unsubscribeToFirebaseTopic;
}
