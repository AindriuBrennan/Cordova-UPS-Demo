import { AbstractPushRegistration } from "../AbstractPushRegistration";
import { OnMessageReceivedCallback, PushRegistrationWebpushOptions } from "../PushRegistration";
import { PushInitConfig, PushVariant } from "../PushInitConfig";
/**
 * AeroGear UPS registration SDK - Webpush implementation
 *
 * Usage:
 * // Initialize SDK first
 * app.init(config);
 * let registration = new PushRegistration();
 * registration.register("myAppleOrFirebaseToken");
 */
export declare class PushRegistrationWebpushImpl extends AbstractPushRegistration {
    static onMessageReceived(onMessageReceivedCallback: OnMessageReceivedCallback): void;
    private static onMessageReceivedCallback;
    constructor(config: PushInitConfig);
    register(options?: PushRegistrationWebpushOptions): Promise<void>;
    unregister(): Promise<void>;
    getPlatformConfig(pushConfig: PushInitConfig): PushVariant | undefined;
    protected validateConfig(pushConfig: PushInitConfig): string | undefined;
    private waitForServiceWorkerToBeReady;
    private subscribeToWebPush;
}
