import { PushRegistrationInterface, PushRegistrationOptions, PushRegistrationWebpushOptions } from "./PushRegistrationInterface";
import { PushInitConfig } from "./PushInitConfig";
export declare type OnMessageReceivedCallback = (notification: any) => void;
export { PushRegistrationOptions, PushRegistrationWebpushOptions } from "./PushRegistrationInterface";
/**
 * AeroGear UPS registration SDK
 *
 * Usage:
 * // Initialize SDK first
 * app.init(config);
 * let registration = new PushRegistration();
 * registration.register("myAppleOrFirebaseToken");
 */
export declare class PushRegistration implements PushRegistrationInterface {
    /**
     * Register the callback that will receive the push message
     * @param onMessageReceivedCallback
     */
    static onMessageReceived(onMessageReceivedCallback: OnMessageReceivedCallback): void;
    private readonly delegate;
    constructor(config: PushInitConfig);
    /**
     * Register the application to the UPS.
     * @param options
     */
    register(options?: PushRegistrationOptions | PushRegistrationWebpushOptions): Promise<void>;
    /**
     * Unregister the application from the UPS.
     */
    unregister(): Promise<void>;
}
